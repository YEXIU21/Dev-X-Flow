# Track all EXE files with LFS by default
*.exe filter=lfs diff=lfs merge=lfs -text

# EXCEPTION: Web download folder - must NOT be LFS so Vercel can serve the binary
# The EXE is 81MB which is under GitHub's 100MB limit for regular files
devxflow-web/frontend/public/download/*.exe -filter -diff -merge text
